[project]
name = "story-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "openai>=3.1.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
story-analyzer = "story_analyzer:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
